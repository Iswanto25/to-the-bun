services:
    boilerplate-bun-elysia-worker:
        image: boilerplate-bun-elysia:latest
        container_name: boilerplate-bun-elysia-worker
        env_file:
            - .env
        environment:
            DOCKER_ENV: "true"
            NODE_ENV: ${NODE_ENV:-production}
        command: ["bun", "run", "worker:start"]
        extra_hosts:
            - "host.docker.internal:host-gateway"
        restart: unless-stopped
