{
	"name": "boilerplate-bun-elysia",
	"version": "1.0.0",
	"description": "Bun + ElysiaJS backend boilerplate with RBAC, JWT auth, BullMQ jobs, Bun-native S3 & Redis",
	"main": "src/app.ts",
	"type": "module",
	"scripts": {
		"prettier": "prettier . --write",
		"lint": "eslint \"src/**/*.ts\" \"__tests__/**/*.ts\" \"scripts/**/*.ts\" \"prisma/**/*.ts\" \"prisma.config.ts\"",
		"typecheck": "tsc --noEmit",
		"lint:fix": "eslint \"src/**/*.ts\" \"__tests__/**/*.ts\" \"scripts/**/*.ts\" \"prisma/**/*.ts\" \"prisma.config.ts\" --fix",
		"pretest": "bun run scripts/prepare-test-env.ts",
		"start": "bun run dist/app.js",
		"dev": "bun --watch src/dev.ts",
		"worker:dev": "bun --watch src/worker.ts",
		"worker:start": "bun run dist/worker.js",
		"start:migrate": "prisma migrate deploy && bun run start",
		"build": "bun build src/app.ts --outdir=dist --target=bun --packages=external && bun build src/worker.ts --outdir=dist --target=bun --packages=external",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"test:coverage": "bun test --coverage",
		"test:integration": "bun test __tests__/integration/",
		"test:infra": "bun test __tests__/infrastructure/",
		"generate-api-key": "bun run scripts/generateApiKey.ts",
		"generate-data": "bun run src/utils/generateData.ts",
		"cleanup": "bash scripts/cleanup-boilerplate.sh"
	},
	"author": "",
	"license": "ISC",
	"engines": {
		"bun": ">=1.4.0"
	},
	"prisma": {
		"seed": "bun run prisma/seed.ts"
	},
	"dependencies": {
		"@elysiajs/cors": "^1.4.0",
		"@prisma/adapter-pg": "7.4.2",
		"@prisma/client": "7.4.2",
		"@types/pg": "^8.18.0",
		"bullmq": "^5.76.6",
		"dotenv": "^17.0.0",
		"elysia": "^1.4.19",
		"jsonwebtoken": "^9.0.2",
		"nodemailer": "^7.0.10",
		"pg": "^8.19.0",
		"pino": "^10.1.0",
		"pino-pretty": "^13.1.2",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@faker-js/faker": "^8.4.1",
		"@types/bun": "latest",
		"@types/jsonwebtoken": "^9.0.10",
		"@types/node": "^24.9.1",
		"@types/nodemailer": "^7.0.11",
		"eslint": "^9.39.1",
		"prettier": "^3.8.3",
		"prisma": "7.4.2",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.56.1"
	},
	"testScenarios": {
		"utils": [
			"Encryption utilities encrypt and decrypt payloads using DATA_ENCRYPTION_KEY.",
			"Generic utility helpers (strings, passwords, OTP, validation) behave deterministically under controlled randomness.",
			"JWT utilities sign and verify access/refresh tokens with provided secrets.",
			"Response helpers log payloads and format JSON responses when dependencies succeed or fail.",
			"S3 helpers validate inputs, upload/delete objects, and surface S3 Storage configuration errors.",
			"SMTP helper builds transporter configuration and propagates send failures.",
			"Token store persists, loads, and deletes tokens using Redis."
		]
	}
}
